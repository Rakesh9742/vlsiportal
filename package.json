{
  "name": "vlsi-portal",
  "version": "1.0.0",
  "description": "VLSI Portal for Students and Teachers",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["vlsi", "portal", "react", "node", "mysql"],
  "author": "VLSI Portal Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 